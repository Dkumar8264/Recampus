{
  "name": "recampus",
  "version": "0.1.0",
  "private": true,
  "description": "Recampus MERN lost and found plus marketplace app.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
